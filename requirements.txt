# Install CUDA-enabled torch/torchvision from the official PyTorch index when
# running GPU experiments. These ranges match the tested torch 2.4.x stack.
torch>=2.4,<2.5
torchvision>=0.19,<0.20

diffusers==0.30.1
numpy>=2.0,<3.0
pillow>=10.0,<13.0
scikit-image>=0.22,<0.26
tqdm>=4.60,<5.0
